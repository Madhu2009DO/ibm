# ${HOME} will be replaced by user home directory according to platform
default_userdir="${HOME}/.ibm/dev"
default_mac_userdir="${HOME}/Library/Application Support/ibm/dev"

default_options="-J-Xms24m -J-Xmx128m --branding ibm -J-Dit.tidalwave.netbeans.disableTabs=true -J-Dit.tidalwave.netbeans.disableQuaua=false"

# default location of JDK/JRE, can be overridden by using --jdkhome <dir> switch
#jdkhome="/path/to/jdk"
